---
name: job-seek-planner
description: >-
  中国大陆机器人/具身智能算法岗求职规划：调研 VLA、具身大模型、强化学习、运动控制（MPC/WBC/阻抗力控）、
  机械臂 manipulation/抓取、sim2real 仿真、SLAM 等方向的公司与岗位，产出带来源的岗位清单、
  单文件 HTML 看板、Excel 表和面试准备清单。适用于「找机器人的工作」「具身智能公司有哪些」
  「VLA 岗位」「算法岗投递清单」「秋招/春招/实习规划」「薪资和 HC 情况」「面经考点」
  「投递进度追踪」等请求。先做简短意图确认，再开始调研。
---

# 机器人算法岗求职规划器（中国大陆）

把一次简短的意图确认，变成**可核实来源**的岗位清单 + HTML 看板 + Excel + 面试准备清单。
专注方向：VLA / 具身大模型、强化学习（RL）、运动控制、整臂与全身控制（WAM / WBC）、
manipulation 与抓取、sim2real 仿真、感知与 SLAM、机器人系统与嵌入式。

流程：**意图确认 → 公司发现 → 并行分片调研 → 合并去重 → 质量门禁 → 看板/Excel → 面试准备与投递节奏**

配套文件（按需读，不用全读）：

- `reference/roles.md`：方向词典、JD 关键词、各方向面试考点；
- `reference/companies.md`：国内公司地图（起点清单，必须现场核实在招情况）；
- `reference/sources.md`：调研渠道、来源分级、不许编造的红线。

下面用 `<skill>` 指本技能目录，`<out>` 指本次求职的输出目录（例如 `./job` 或
`./job_search/2026秋招`）。

## 第 0 步：选定输出目录

`<out>` 下会有：`jobs.json`（唯一数据源）、`report.html`（看板）、`jobs.xlsx`（可选）、
`interview_prep.md`（可选）。刷新时**在 jobs.json 上增量更新**，不要重建覆盖用户已经填的
`投递状态` 和 `备注`。

## 第 1 步：意图确认（最多问 5 个问题，先问再查）

一次问一组，别一口气抛问卷。必须问清：

1. **方向与岗位**：VLA/具身大模型、RL、运动控制、manipulation/抓取、仿真 sim2real、感知/SLAM、
   嵌入式/系统，多选；目标岗位名（算法工程师 / 研究员 / 具身智能算法 / 控制算法 / 机器人算法）。
2. **身份与时间线**：应届（26 届/27 届秋招、春招）、实习、社招；期望到岗时间；学历与研究/项目背景
   （只记录能改变匹配判断的部分，不要收集身份证号等隐私）。
3. **城市**：北京、上海、深圳、杭州、苏州、南京、广州、成都、合肥等；能否接受异地；是否接受
   工厂/产线驻场。
4. **硬条件**：薪资底线（月薪 ×N 个月 或 年包，说清口径）、公司类型偏好（大厂 / 独角兽 /
   早期创业 / 国企院所 / 高校实验室工程岗）、排除项（如纯外包、纯 demo 公司、长期出差）。
5. **交付偏好**：要不要 Excel、要不要面试准备清单、看板语言（默认中文）、要覆盖多少家公司
   （**默认 20–25 家，最多 30 家**）。给用户的选项按这个梯度：先跑一批 10 家 / 标准 15–20 家 /
   全套 20–25 家 / 尽量全 30 家。注意单位是**公司**不是岗位：每家公司都要把符合方向的岗位**找全**，
   一家开 15 个对口岗就记 15 条，一家只有 1 个就记 1 条。

   选 30 家时提醒用户：目标城市的对口公司可能不足 30 家，这时不要为了凑数把方向不符的公司塞进来，
   宁可少列并说明「该城市符合方向的公司只有 N 家」。

把确认结果写进 `<out>/jobs.json` 的 `meta` 里（`方向`、`身份`、`城市`、`薪资底线`、`口径`、
`目标公司数`、`更新时间`、`排除项`）。**用户确认之前不要开始大规模检索。**

## 第 2 步：调研

参考 `reference/companies.md` 起手，用 `reference/roles.md` 的关键词组合检索，遵守
`reference/sources.md` 的来源分级和其中的**招聘系统检索技巧**。

**第一件事是把公司找全，而不是直接查 HC。** `reference/companies.md` 是静态清单，一定会漏掉近一年
成立的新公司，所以每轮都要先跑那份文件里的「公司发现流程」：机器人顶会（ICRA / IROS / CoRL / RSS）
的赞助商与展商名单、arXiv cs.RO 论文的公司 affiliation、WRC 等展会展商名录、按月的融资盘点新闻、
投资机构 portfolio、GitHub/HF 组织、工商近 12 个月新注册。把发现的新公司并进清单后再往下走。

**调研单位是公司，不是岗位。** 按城市和公司类型确定目标公司清单（默认 20–25 家，最多 30 家），逐家过一遍；
每家公司都要在它自己的招聘系统里把符合用户方向的岗位**穷尽**，不要每家只挑一个代表岗：

- 用「算法」「具身」「强化学习」「运动控制」「机械臂」「仿真」「操作」等词**分别**检索一遍，合并去重；
  只用一个词搜一遍就换公司，是最常见的漏岗原因；
- 能筛城市 / 职能就先筛，再翻页，把命中的岗位全部记下来（同一岗位的普通批与「优才/专项」批算两条）；
- 一家公司确实只有 1 个对口岗，就只记 1 条；一家都没有，也要在 `jobs.json` 里留一条
  `hc状态: 未知` 的记录说明查过了，不要让它从清单里消失。

**并行分片调研（公司多时的标准做法）**

不许因为「调用次数多」「时间长」就只深挖几家、其余记 `未知` 草草收场。清单上的每一家都要挖到底，
公司多就并行：**用 subagent 分批，每个 agent 负责 3–5 家公司**，产出分片文件，再由主流程合并。

**运行目录布局**（`<out>` 为本次求职的工作目录）：

```
<out>/
  jobs.json            # 合并后的唯一数据源
  agent_out/           # 各 agent 的分片研究文件
    batch_a.json  batch_b.json  batch_c.json  ...
  report.html  jobs.xlsx  interview_prep.md
  status.json          # 看板导出的投递状态（用户数据，永不被覆盖）
```

**分批**：20 家公司 → 4–5 个 agent；30 家 → 6–8 个。按公司类型分组（大厂 / 头部创业 / 早期 /
院所平台），同类公司的招聘系统和检索套路相近，agent 内部可复用经验。

**分片契约**：每个 agent 必须把结果**写成 JSON 文件落盘**，不要只在对话里汇报（转述会丢信息）：

```json
{"jobs": [ ...岗位记录... ],
 "coverage": {"公司名": {"检索词": [], "命中": 12, "收录": 8, "筛掉": 4, "说明": ""}},
 "errors": [{"code": "blocked|not_found|timeout|parse_error", "message": "哪个源没拿到、为什么"}]}
```

agent 的 prompt 必须自带：目标方向标签、城市、届别、字段 schema、来源分级规则、
`reference/sources.md` 里的招聘系统检索技巧、以及「查不到也要写 `hc状态: 未知` 并说明查了哪些渠道」。
已经掌握的线索（官网地址、已知公告、失败过的域名）要写进 prompt，避免 agent 重复踩坑。

**合并**：

```bash
python3 "<skill>/assets/merge_shards.py" "<out>/agent_out" "<out>/jobs.json"
```

合并器保证四件事：

1. **稳定身份去重**：公司 + 职位ID（无 ID 时用 JD 链接里的 position id，再退化到 公司+岗位名+城市 的
   归一化串）。不拿数组顺序或岗位名单独做主键——同名岗位在普通批和优才批是两条。
2. **用户状态不可覆盖**：`投递状态`、`下一步`、`备注` 一律以本地已有的为准，刷新不会冲掉。
3. **失败不静默丢弃**：分片里的 `errors[]` 汇总进 `meta.研究错误`；某个源打不开要记 `访问失败`，
   不能让这家公司从清单里消失。
4. **覆盖度对账**：按实际条数校正每家的 `收录`，agent 自报数与表里对不上时以表为准并留痕。

**合并后跑质量门禁**：

```bash
python3 "<skill>/assets/build_report.py" "<out>/jobs.json" --check-only        # 或加 --strict
```

先修错误再产出看板，覆盖度缺口和缺失的覆盖记录会以警告形式列出来。

**❌ 禁止静默截断（最容易犯的错）**

凡是命中用户方向标签的在招岗位，**全部入表**。不许"挑最对口的几个""先放几个代表岗"——
用户看到的是"这家公司只有 2 个岗"，而不是"我只写了 2 个"，这会直接影响他判断投哪家。

只有三种情况可以不收录，且必须在覆盖记录里体现数量：

1. 方向明显不符（例如 HR、销售、结构件、纯硬件）；
2. 同一岗位的重复发布（同名同城同批次）；
3. 岗位详情完全拿不到（记为 `访问失败`，不是"没有"）。

**每家公司必须在 `meta.公司覆盖` 里留一条结构化记录**，不要写进 `存疑` 的自由文本里：

```json
"公司覆盖": {
  "智元机器人 AgiBot": {
    "检索词": ["算法", "具身", "强化学习", "运动控制"],
    "命中": 41, "收录": 41, "筛掉": 0,
    "说明": "上海·校招·算法类全部收录"
  },
  "某公司": {"检索词": ["算法"], "命中": 12, "收录": 9, "筛掉": 3, "说明": "3 个为纯硬件岗，方向不符"}
}
```

`收录 < 命中 - 筛掉` 时校验器会报警告，看板的「公司覆盖度」区也会把缺口显示出来。
覆盖记录缺失同样会告警——**不写覆盖记录，等于默认你没查全**。

逐家公司要查的内容：

1. **在招确认**：公司官方招聘页 / 招聘系统（飞书招聘、Moka、北森、智联企业版）、官方公众号招聘推文、
   BOSS 直聘 / 拉勾 / 实习僧 上的官方账号；记录 JD 链接、发布或更新时间。
2. **岗位要求**：核心要求、加分项、学历经验门槛、技术栈（ROS2、Isaac Sim/Gym、MuJoCo、
   PyTorch、C++、实机平台）。
3. **薪资（必须单独跑一轮，不要顺带看）**：国内 JD 基本不写薪资区间，所以薪资要专门检索，顺序是：

   - **JD 明示区间**（少见，有就是 `官方JD` + 可信度 `高`）；
   - **牛客网「查薪资」板块**：`nowcoder.com/search?type=salary&query=<公司名>`，是用户自报 offer 的结构化
     记录（公司/岗位/薪资/学历），**必须用浏览器打开**，WebFetch 只能拿到导航菜单；
   - **牛客 offer 帖 / offer 帮选帖**：`?type=post&query=<公司名> offer 薪资`，常见「n×16」「40+」这类口径；
   - 一亩三分地、脉脉、offershow（多数要登录，拿不到就记 `访问失败`）；
   - 媒体的赛道薪酬盘点（只能当**行业锚点**，绝不能安到具体公司头上）。

   写入规则：牛客/社区数据一律 `薪资来源: 网传` + `薪资可信度: 低`（多条互相印证可以给 `中`）；
   媒体推算或类比锚点用 `推断`；查过但没有记录的，**薪资仍写 `未知`，并在 `存疑` 里写明
   「牛客薪资库<日期>检索无记录」**——「查过没有」和「没查」对用户是两件完全不同的事。
   还要记清口径：月薪×几个月、是否含签字费/股票、是否区分白菜与 SSP、是几年前的数据。
4. **公司情况**：融资轮次与时间、产品与实机进展、团队负责人与技术路线、稳定性信号
   （裁员、业务调整、社招冻结）。
5. **面试流程**：笔试/OA 形式、几轮技术面、是否有实机/仿真作业、HR 面、平均周期（来自当期面经）。
6. **匹配度**：结合用户背景给 0–10 分和一句话理由，理由要指向具体要求，不要空话。

同一家公司的多个岗位可以共用公司级信息（阶段、团队、来源、面试流程），但 `岗位`、`方向标签`、
`核心要求`、`匹配度`、`匹配理由` 必须逐岗写，不要复制粘贴同一段话。

每条**会影响投递决定**的信息（薪资、在招状态、截止时间、地点、是否接受应届）都要在
`来源` 里留下 `{标题, 链接, 类型, 访问日期}`，并在 `存疑` 里记下没查实的部分。

## 第 3 步：写 `jobs.json`

数据模板见 `assets/jobs_template.json`。核心字段：

```json
{
  "meta": {"方向": ["VLA", "RL"], "身份": "27届秋招", "城市": ["北京", "上海"],
           "薪资底线": 30, "口径": "月薪(k)×15", "更新时间": "2026-08-20", "排除项": []},
  "jobs": [
    {
      "id": "unitree_rl_bj",
      "公司": "示例机器人", "岗位": "具身智能算法工程师（VLA）", "团队": "大模型组",
      "方向标签": ["VLA", "manipulation"], "城市": "北京", "工作地址": "海淀区…",
      "hc状态": "在招", "投递渠道": "官网校招", "jd链接": "https://…", "截止时间": "2026-10-31",
      "薪资": "35-50k×15", "薪资来源": "网传", "薪资可信度": "低",
      "学历要求": "硕士及以上", "经验要求": "应届",
      "核心要求": ["Diffusion Policy/ACT 等模仿学习", "真机数据采集经验"],
      "加分项": ["顶会一作", "OpenVLA 复现"],
      "面试流程": "OA(算法2题) → 技术面×2 → 交叉面 → HR",
      "公司阶段": "B轮，2026-03", "团队信息": "负责人来自…",
      "匹配度": 8.5, "匹配理由": "你的 RL+真机抓取经历直接对口",
      "投递状态": "未投", "下一步": "改简历里的抓取项目描述", "备注": "",
      "来源": [{"标题": "官网校招页", "链接": "https://…", "类型": "官方", "访问日期": "2026-08-20"}],
      "存疑": ["薪资仅来自当期牛客帖，未经官方确认"]
    }
  ]
}
```

枚举值（校验会检查）：

- `hc状态`：`在招 | 暂停 | 已关闭 | 未知`
- `薪资可信度`：`高 | 中 | 低 | 未知`（`高` 只给 JD 或官方明示区间）
- `薪资来源`：`官方JD | 官方公告 | 网传 | 推断 | 未知`
- `投递状态`：`未投 | 已投 | 笔试 | 一面 | 二面 | 三面 | HR面 | Offer | 挂 | 暂缓`

## 第 4 步：生成看板与 Excel

```bash
python3 "<skill>/assets/build_report.py" "<out>/jobs.json" --html "<out>/report.html" --xlsx "<out>/jobs.xlsx"
```

脚本会先校验（必填字段、枚举、薪资有没有来源、链接格式），把问题打印出来并写进看板的
「数据质量」区；错误会阻止生成，警告不会。只想校验时加 `--check-only`。

看板的「年包中位数」会标注样本量 `n=`，因为多数岗位薪资未知，中位数只由少数几条撑起来，不标样本量会误导。

`report.html` 是**单文件**，双击即可打开：KPI（含公司数）、按公司/方向/城市/状态筛选、搜索、按匹配度或薪资排序、
岗位卡片、来源链接、存疑标注；投递状态可以在页面上改，存在浏览器 localStorage 里，
点「导出状态」得到一段 JSON，可以粘回 `jobs.json`（脚本 `--merge-status status.json` 也能合并）。

## 第 5 步：面试准备与投递节奏

按 `reference/roles.md` 里对应方向的考点，给出：

- 每个方向的必背清单（例如控制：动力学建模、LQR/MPC、阻抗/导纳控制、WBC、ZMP/MPC 步态；
  RL：PPO/SAC、reward shaping、domain randomization、sim2real；
  VLA：ACT/Diffusion Policy/RT-2/OpenVLA/π0、数据采集与遥操作、失败模式）；
- 项目讲述框架（问题 → 方案 → 你的贡献 → 量化结果 → 失败与改进）；
- 一周投递节奏建议（先投 2–3 家练手，主目标放在简历和项目描述改完之后）。

可写入 `<out>/interview_prep.md`。

## 红线（必须遵守）

1. **不编造**：薪资、HC、截止时间、面试流程、公司融资，查不到就写 `未知` 并留在 `存疑` 里；
   打不开的页面记成访问失败，不等于岗位不存在。
2. **来源分级**：官方 JD/官网 > 官方公众号 > 招聘平台官方账号 > 牛客/脉脉/一亩三分地当期帖 >
   模型记忆（模型记忆只能当检索线索，不能当结论）。薪资来自社区帖必须标 `网传`。
3. **时效**：机器人行业公司变动快，所有结论都写清访问日期；超过 30 天的数据提示复查。
4. **隐私**：只用公开的职业信息（官网团队页、公开主页、公众号）。不收集私人手机号、私人邮箱、
   住址，不猜邮箱格式。可以帮用户**起草**内推私信或邮件，但**不代发**。
5. **不做承诺**：匹配度是建议不是结论；不替用户判断能不能拿到 offer。
